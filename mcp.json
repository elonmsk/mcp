{
    "name": "indra_variants",
    "version": "1.0.0",
    "description": "MCP server that retrieves DBSNP variants associated with genes via Indra API.",
    "tools": [
      {
        "name": "get_variants_for_gene",
        "description": "Get DBSNP variants associated with a gene using the Indra Discovery API.",
        "input_schema": {
          "type": "object",
          "properties": {
            "gene": {
              "type": "array",
              "items": {"type": "string"},
              "description": "Tuple [namespace, id], e.g. ['HGNC', '9896']"
            }
          },
          "required": ["gene"]
        }
      }
    ]
  }
  